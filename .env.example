# Example env for Next app (do NOT commit secrets)
# Copy to .env.local and set real values for production

# If empty, API route will fallback to mock responses during development
NEXT_RIVHIT_API_TOKEN=yo

# Force using mock responses regardless of token (development convenience)
